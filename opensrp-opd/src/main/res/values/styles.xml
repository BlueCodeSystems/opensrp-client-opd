<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar" />

    <!-- AlertDialog themes used in code -->
    <style name="AppThemeAlertDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog" />
    <style name="OpdDialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog" />

    <!-- Fullscreen dialog style used by pickers -->
    <style name="FullScreenDialogStyle" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
